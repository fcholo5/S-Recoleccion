name: 🚀 Deploy Angular Frontend to Apache2 VM

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # ----------------- CHECKOUT -----------------
      - name: 🧩 Checkout código
        uses: actions/checkout@v4

      # ----------------- NODE -----------------
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ----------------- DEPENDENCIAS -----------------
      - name: 📦 Instalar dependencias frontend
        run: |
          cd frontend
          npm ci
          npm install -g @angular/cli
          cd ..

      # ----------------- BUILD FRONTEND -----------------
      - name: 🏗️ Compilar frontend Angular
        run: |
          cd frontend
          ng build --configuration production --base-href /
          cd ..

      # ----------------- PREPARAR DEPLOY -----------------
      - name: 🧩 Preparar archivos frontend
        run: |
          rm -rf deploy-frontend
          mkdir deploy-frontend
          cp -r frontend/dist/frontend/browser/* deploy-frontend/
          cp frontend/dist/frontend/3rdpartylicenses.txt deploy-frontend/ || true
          cp frontend/dist/frontend/prerendered-routes.json deploy-frontend/ || true

      # ----------------- DEBUG -----------------
      - name: 🧾 Ver contenido listo para deploy
        run: ls -R deploy-frontend

      # ----------------- LIMPIAR SERVIDOR -----------------
      - name: 🧹 Limpiar contenido anterior en el servidor
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          script: |
            rm -rf /var/www/fabian_site/*
            echo "✔ Carpeta limpiada en servidor"

      # ----------------- SUBIR ARCHIVOS -----------------
      - name: 📤 Subir frontend al servidor Apache2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          source: "deploy-frontend/*"
          target: "/var/www/fabian_site/"
          overwrite: true

      # ----------------- SSH TASKS -----------------
      - name: ⚙️ Ajustar permisos finales
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          script: |
            echo "✔ Deploy completado"

            
            